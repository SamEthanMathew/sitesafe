# SiteSafe — runtime dependencies
# Pinned to lower bounds; tested on Python 3.10+

gradio>=4.0.0
ollama>=0.3.0
Pillow>=10.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
fpdf2>=2.7.0
pandas>=2.0.0
tqdm>=4.65.0
joblib>=1.3.0
requests>=2.31.0

# Optional — only required for the training pipeline
# (kept here for one-shot install convenience; comment out if you only run the app)
# unsloth>=2024.5
# trl>=0.9.0
# transformers>=4.40.0
# torch>=2.2.0
# datasets>=2.18.0
# accelerate>=0.30.0
# peft>=0.10.0
# bitsandbytes>=0.43.0
